# Infrastructure module environment

# Compose profile selection (comma separated)
COMPOSE_PROFILES=local

# Optional local image tags
BACKEND_IMAGE_TAG=local
ML_SERVICE_IMAGE_TAG=local

# Optional host port overrides
BACKEND_PORT=3000
ML_SERVICE_PORT=8000
EVM_RPC_PORT=8545
