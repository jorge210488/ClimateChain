# Shared Module

Reusable assets across modules.

## Planned Contents

- `abi/` contract ABI artifacts consumed by backend
- `schemas/` shared JSON schema or typed contracts
- `constants/` shared application constants

Keep shared assets stable and versioned to avoid integration drift.
