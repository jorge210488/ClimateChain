# ClimateChain ML service dependencies.
#
# Stage 01 established the baseline; Stage 07 adds the serving stack and the
# model runtime. The training pipeline is Stage 08, which extends this file
# rather than replacing it.
#
# Pins are lower bounds on purpose: the lockfile equivalent arrives with the
# Stage 12 container build, where the runtime image is what needs to be exact.

# Configuration and validation shared by every later stage.
# pydantic floor is 2.11: earlier releases ship no pydantic-core wheel for
# Python 3.13 and fall back to a Rust source build that fails on a clean machine.
pydantic>=2.11,<3
pydantic-settings>=2.8,<3
python-dotenv>=1.0,<2

# Serving stack (Stage 07).
fastapi>=0.115,<1
uvicorn>=0.34,<1

# Fitting the baseline artifact (scripts/build_baseline_model.py). Not imported
# by any runtime path: the service loads coefficients and evaluates them in
# plain Python, so serving needs no numerical stack.
numpy>=2.1,<3

# Test tooling, so `tests/` is runnable from Stage 01 onward.
pytest>=8.3,<9
httpx>=0.28,<1

# Lint and format, enforced by the Stage 07 gate.
ruff>=0.9,<1
