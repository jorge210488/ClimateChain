# Stage 08 - Data Pipeline and Model Training

## Scope completed

- **Objective achieved:** The pricing model is now trained from thirty years
  of observed daily rainfall for every region the service knows, by a script
  that reproduces the committed artifact byte for byte from a committed,
  checksummed dataset. The artifact records its provenance — dataset version
  and checksum, source, split, configuration hash, and the hash of the trainer
  itself — and the runtime reports that provenance on readiness. The synthetic
  Stage 07 model is archived and scored on the same held-out years, so the
  improvement is a measured comparison rather than a claim.
- **Purpose and value:** Stage 07 proved the service could load and serve a
  model; it could not claim the model priced real risk, because the data behind
  it was drawn from a distribution somebody chose. This stage replaces the
  evidence behind the coefficients without touching the artifact contract or
  the serving code, and does it in a way an auditor can rerun: dataset in the
  repository, training deterministic, metrics regenerated by the gate. The
  time-based evaluation is what makes the number honest — the model is scored
  on 2019-2024, which it never saw, and the synthetic model turns out to have
  under-priced that period by more than four times.
- **Functional result:** `python scripts/train_rainfall_model.py` retrains
  `baseline-premium-v2` from `data/rainfall-history-v1.json` in under a second
  and produces the committed artifact and metrics file unchanged.
  `GET /health/ready` now reports `datasetVersion`, `trainingKind`, and
  `transitional` alongside the model version and checksum. `POST /predict`
  prices from the observed model with no change to the request or response
  contract. `python scripts/stage8_check.py` is the gate: lint, format,
  dataset integrity, retrain-and-diff for artifact and metrics, 226 tests,
  and a real process boot.
- **Integrated previous-stage outputs:** The Stage 07 runtime is consumed
  unchanged as the evaluator — `app/models/baseline.assess_risk` scores both
  the new and the archived model on the holdout, so the metrics describe the
  deployed code path. The Stage 07 artifact loader validates every artifact
  the trainer writes, and the Stage 07 gate (`stage7_check.py`) still runs as
  a subset. The backend's `docs/api/backend-openapi.json` (Stage 05) and
  `policy.constants.ts` (Stage 05/06) continue to be read by the contract
  tests; the shared pricing vectors (`shared/contracts/`) still pass on both
  sides. The backend's chain-free gate was rerun after the change to confirm
  nothing on the consumer side moved.
- **Runtime data sources (real vs test-only):** Real — the model artifact,
  now trained on ERA5 reanalysis (assimilated observations at ~10 km, served
  as daily precipitation sums by the Open-Meteo Historical Weather API,
  CC BY 4.0). The artifact is tagged `kind: "observed"`,
  `transitional: false`, and the gate refuses an artifact that says otherwise.
  Test-only — nothing; the tests exercise the committed dataset and the
  committed artifact. No runtime path reaches the network: the fetch is a
  deliberate, separately-run script.
- Retained from Stage 07 by design: the model family (log-odds of trigger
  frequency against log threshold, log duration, and region wetness), the
  artifact schema version, the 0.35 premium loading, the eight regions, and the
  premium floor. What changed is the data and the evaluation.

## Files changed

New:

- `ml-service/data/regions.json` — the region registry: eight regions with
  the coordinates they are fetched at, keyed by the canonical form the
  pricing code matches on.
- `ml-service/data/rainfall-history-v1.json` — the dataset: 8 regions ×
  10,958 days (1995-01-01 to 2024-12-31), one decimal of millimetres,
  provenance block, SHA-256 checksum. 351 KB, committed.
- `ml-service/app/data/__init__.py`, `ml-service/app/data/rainfall.py` —
  dataset loader: checksum verification, shape and day-count validation,
  rejection of negatives, missing days, duplicate keys, and non-standard JSON
  constants; date ↔ index helpers shared by training and tests.
- `ml-service/scripts/fetch_rainfall_history.py` — the only network step.
  Fetches every registered region, refuses short, gappy, negative, or
  wrongly-united series rather than patching them, and writes the dataset
  with a checksum. A refresh that changes nothing leaves the file
  byte-identical.
- `ml-service/scripts/train_rainfall_model.py` — training: time-based split,
  non-overlapping windows, deterministic least-squares fit, evaluation of the
  new and the archived model on the same holdout with the runtime's evaluator,
  provenance and configuration hash, byte-stable output.
- `ml-service/scripts/stage8_check.py` — the Stage 08 gate.
- `ml-service/app/models/artifacts/baseline-premium-v2.json` — the artifact.
- `ml-service/app/models/artifacts/baseline-premium-v2.metrics.json` — the
  holdout metrics and the previous-model comparison, regenerated by the gate.
- `ml-service/tests/test_training_pipeline.py` — 25 tests across dataset
  integrity, fetch validation, training, evaluation, and runtime provenance.
- `docs/stage-reports/stage-08-data-pipeline-and-model-training.md` — this
  report.

Moved and removed:

- `ml-service/app/models/artifacts/baseline-premium-v1.json` →
  `ml-service/app/models/artifacts/archive/baseline-premium-v1.json`. Kept so
  the holdout comparison is reproducible; the gate fails if it is missing.
- `ml-service/scripts/build_baseline_model.py` — removed. The synthetic
  generator has no remaining purpose; the archived artifact it produced is
  what the comparison needs, and its checksum is recorded in the metrics.

Modified:

- `ml-service/app/models/artifact.py` — `ModelArtifact` gains optional
  `dataset_version`, `training_kind`, `transitional`; the loader parses an
  optional `training` block and rejects a malformed one. An artifact without
  the block still loads, so the archived Stage 07 artifact remains readable.
- `ml-service/app/models/registry.py`, `ml-service/app/schemas/health.py`,
  `ml-service/app/api/routes.py` — readiness carries the three provenance
  fields.
- `ml-service/app/core/config.py`, `ml-service/.env.example` — default
  `MODEL_PATH` is the v2 artifact; the data-provider comment reflects that the
  source is keyless.
- `ml-service/scripts/stage7_check.py`, `ml-service/scripts/startup_check.py`,
  `ml-service/tests/conftest_helpers.py`, `ml-service/tests/test_api.py`,
  `ml-service/tests/test_backend_contract.py`,
  `ml-service/tests/test_config_and_money.py` — repointed from v1 and the
  build script to v2 and the trainer.
- `ml-service/tests/test_pricing.py` — `test_the_floor_compresses_low_risk_quotes`
  now uses inputs where the floor genuinely dominates on the observed model
  (120 mm over one month, Lima vs Sevilla). At 50 mm over six months the
  synthetic model floored both regions; the observed one prices them apart,
  which is the model being more informative, not the floor breaking.
- `ml-service/README.md` — model, evaluation, dataset, commands, and gate
  sections rewritten for the observed model.
- `.github/workflows/ml-service-quality-gates.yml` — job runs
  `stage8_check.py`; the drift step's message updated.
- `.gitignore` — keeps `app/models/artifacts/archive/*.json`.
- `AGENTS.md`, `README.md` — gate command and stage status updated;
  the Quick Start no longer asks for a build step the artifact does not need.

## Decisions made

- **Source: Open-Meteo's ERA5 archive, not a keyed provider.** It is open,
  keyless, licensed for this use, and serves a reanalysis product that is
  observationally constrained at every grid point — including the regions in
  Latin America where station records are sparse or paywalled. The
  `WEATHER_API_*` variables stay declared and empty for a provider that is
  not. Choosing a keyed provider would have made the pipeline depend on a
  credential the repository's policy says not to commit, for no gain in data
  quality at this resolution.
- **The dataset is committed, not fetched by the gate.** 351 KB is a small
  price for a gate that cannot fail on a remote service's availability, and
  for a training run an auditor can repeat without an account. The fetch is
  a separate, deliberate act, and a refresh that returns identical data
  leaves the file byte-identical so `git status` reports real change only.
- **Split by calendar, not at random.** Windows overlap in time; a random
  split of windows would leak the holdout's weather into training and
  flatter the model. Fitting on 1995-2018 and scoring on 2019-2024 is the
  test a deployed model actually faces: pricing years it has not seen.
- **Non-overlapping windows.** Trigger frequency is measured on windows tiled
  end to end, not slid one day at a time. Sliding windows would count the
  same storm dozens of times and make the sample look far larger than it is.
- **Region risk is measured on the training period only.** Using the full
  series would let the holdout years inform a feature the holdout is then
  scored on.
- **The runtime's evaluator scores the holdout.** `assess_risk` is the code
  that prices; scoring with it means the metrics describe the deployed path
  rather than a re-implementation that could drift from it.
- **Proper scoring rules, and the previous model on the same holdout.** Log-
  loss and Brier are what a probability forecast should be judged on. The
  archived synthetic artifact is scored on identical windows, which is the
  only way "better" means anything; that is why it is kept in the repository
  rather than deleted.
- **The configuration hash covers the trainer itself.** `configHash` includes
  the SHA-256 of `train_rainfall_model.py`, so a change to the fitting code
  changes the artifact even when the numbers happen to survive it. Reformatting
  the trainer during this stage changed the artifact checksum for exactly
  that reason, and the gate caught it.
- **Provenance is additive and optional in the loader.** The Stage 07
  runtime loads the new artifact without modification, and the archived
  artifact without a `training` block still loads. The gate — not the loader
  — is what insists the *current* artifact be observed and non-transitional,
  because an operator may legitimately need to load an old artifact during an
  incident.
- **The loading stays at 0.35 and is not estimated.** It is a commercial
  margin; the data cannot decide it, and dressing it up as a fitted parameter
  would misrepresent a choice as a finding.
- **Model family unchanged.** A more expressive model was not attempted.
  With eight regions and one feature per axis, the honest claim available
  from this data is calibration on the holdout, and the linear log-odds model
  delivers it (predicted trigger rate 0.101 against observed 0.097). A richer
  model belongs to a stage with more regions and a reason to need it.

## Commands executed

- `cd ml-service && python scripts/fetch_rainfall_history.py` — once, on
  2026-09-06, to produce the committed dataset (checksum
  `e04ea058f60108dd02bfa5d21aa7d4e9b62618d11fe7a9148c82a1f6feba21ab`).
- `cd ml-service && python scripts/train_rainfall_model.py` — produces the
  committed artifact (checksum
  `be06eeeb4b0ce448219d74b4d30e89dbe325149ecfdfeb693552115f0e9df3c7`) and
  metrics.
- `cd ml-service && python scripts/stage8_check.py` — the gate.
- `cd ml-service && python scripts/stage7_check.py` — the Stage 07 subset,
  still green.
- `cd backend && npm run stage5:check` — the consumer side, chain-free.
- `cd backend && npm run stage6:check` — attempted; its live-chain e2e step
  requires a running local chain and was not run against one for this stage,
  since no backend or chain code changed. Everything before that step passed.

## Tests executed and results

- `python scripts/stage8_check.py`: **OK.**
  - lint: all checks passed; format: 35 files already formatted.
  - dataset integrity: `rainfall-history-v1`, 8 regions, 10,958 days,
    checksum verified, registry consistent.
  - training drift: retrained `baseline-premium-v2` from
    `rainfall-history-v1`, artifact and metrics byte-identical.
  - tests: **226 passed** (201 from Stage 07 plus 25 new).
  - runtime startup: `/health` 200, `/health/ready` 200 with the v2 checksum
    verified, `/predict` 200 pricing from `baseline-premium-v2`.
- Holdout metrics (2019-2024, 39,680 windows, from
  `baseline-premium-v2.metrics.json`):

  | Model | Data | Log-loss | Brier | Predicted trigger rate | Observed |
  | --- | --- | --- | --- | --- | --- |
  | `baseline-premium-v2` | observed | 0.18227 | 0.055533 | 0.101238 | 0.096951 |
  | `baseline-premium-v1` | synthetic | 0.28830 | 0.080362 | 0.022092 | 0.096951 |

  The new model is better on both proper scores and is calibrated to within
  half a percentage point. The synthetic model predicted a trigger rate of
  2.2% against 9.7% observed: it would have collected roughly a quarter of the
  premium the risk warranted.
- New tests in `tests/test_training_pipeline.py`:
  - Dataset: loads and verifies the committed history; covers exactly the
    registered regions; records where the data came from; looks like real
    climate (Lima the driest, Medellín the wettest, ordering matches known
    climatology); date indexing round-trips; rejects a corrupted, truncated,
    duplicated, or non-finite dataset; rejects a single tampered value.
  - Fetch validation (no network — the fetcher is exercised on an in-memory
    series): payload carries provenance and a checksum; a refresh that changes
    nothing keeps its timestamp.
  - Training: split is by calendar and disjoint; windows do not overlap;
    region risk is measured on the training period only; retraining
    reproduces the committed artifact; coefficient signs are physical;
    holdout metrics are proper and calibrated; beats the synthetic model on
    the same holdout; the synthetic model under-priced real risk; artifact
    metrics match the metrics file; the config hash tracks the trainer.
  - Runtime provenance: current artifact is observed and not transitional;
    the archived synthetic artifact still loads and says so; readiness
    reports provenance; the default model path is the current artifact;
    malformed provenance is rejected; an artifact without provenance still
    loads.
- `npm run stage5:check` (backend): **OK** — 322 unit tests, 34 e2e tests,
  startup check, OpenAPI document up to date. The shared pricing vectors
  passed on both sides.

## Risks or pending items

- **Reanalysis is not a rain gauge.** ERA5 is a model constrained by
  observations at ~10 km; local convective extremes can be smoothed relative
  to what a station at the policy's location records. The oracle that settles
  a policy (Stage 10) will read some specific source, and the pricing data
  should be reconciled with that source once it exists. Until then, the
  model's probabilities are calibrated to ERA5's view of the trigger, not
  necessarily to the oracle's.
- **Eight regions, one point each.** A region is priced from a single
  coordinate. That is the registry's honest scope; expanding it is data work
  (register, refetch, retrain), and the gate will refuse a registry that
  disagrees with the dataset.
- **Unknown regions are priced at the mean risk.** Carried over from
  Stage 07 and now meaning the mean of the eight observed series. It remains
  a placeholder, not a risk assessment.
- **Stationarity is assumed.** The model is fitted on 1995-2018 and scored on
  2019-2024; both periods are in the past. A warming climate shifts extreme
  precipitation, and a model this simple has no term for that. The metrics
  file makes drift visible when the dataset is refreshed and the holdout
  moves; nothing yet acts on it.
- **Extreme thresholds are extrapolated.** Beyond roughly 120 mm/day the
  observed frequency is near zero for every region and the fit is governed
  by the log-threshold slope, not by counts. Quotes at those thresholds are
  the floor's business, as the floor test now documents.
- **No authentication and no rate limiting** — unchanged from Stage 07, and
  unchanged in urgency: Stage 09 defines the topology. `/predict` must not be
  exposed publicly before then.
- **A refreshed dataset changes the artifact.** By design — but it means a
  data refresh is a model release, with the same review a code change gets.

### Credentials status

- `Credentials required now:` No
- `Credentials list:` `WEATHER_API_BASE_URL`, `WEATHER_API_KEY` (declared,
  unused, empty)
- `Purpose:` reserved for a keyed weather provider, should one replace the
  open Open-Meteo archive. Nothing in Stage 08 reads them; the fetch, the
  training, the gate, and the service all run with both empty.

## Next stage handoff notes

- Stage 09 wires the backend's `PricingService` to `POST /predict`, replacing
  its 501. The request and response contract did not change in this stage;
  `tests/test_backend_contract.py` and the shared vectors still hold on both
  sides, so the wiring should be mechanical.
- The readiness payload now carries `datasetVersion`, `trainingKind`, and
  `transitional`. The backend's health aggregation in Stage 09 should surface
  at least `modelVersion` and `transitional`, so a deployment serving a
  transitional or archived model is visible from the backend's own probe.
- `MODEL_PATH` defaults to the v2 artifact. The archived v1 still loads if an
  operator points at it — the *gate* refuses it, the runtime does not — and
  readiness will say `trainingKind: "synthetic"`, `transitional: true`.
- Retraining is `python scripts/train_rainfall_model.py`; refreshing data is
  `python scripts/fetch_rainfall_history.py` followed by retraining. Both
  produce byte-stable output, and the gate fails on any difference from what
  is committed. A dataset refresh is a model release: review the metrics
  diff, not only the checksum.
- Adding a region: add it to `data/regions.json` under its canonical
  lowercase key, refetch, retrain, commit all three. The gate enforces the
  registry ↔ dataset agreement and the coordinates.
- When Stage 10 fixes the settlement oracle's data source, compare its
  historical series against `data/rainfall-history-v1.json` for the same
  regions and dates before trusting the pricing model's calibration against
  it. That comparison is the first thing this stage cannot do yet.
