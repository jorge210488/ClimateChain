# Deployment Manifests

This directory stores deterministic deployment metadata generated by `scripts/deploy.ts`.

## Format

Each network writes a JSON manifest named `<network>.json` with:

- deployment timestamp
- chain ID and network name
- deployer address
- deployed contract addresses

Do not store private keys or sensitive secrets in this directory.
