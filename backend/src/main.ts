function bootstrap(): void {
  // Stage 01 placeholder. NestJS app bootstrap will be implemented in Stage 05.
  // Keeping this entrypoint prevents empty-module confusion.
  // eslint-disable-next-line no-console
  console.log("ClimateChain backend bootstrap placeholder");
}

bootstrap();
